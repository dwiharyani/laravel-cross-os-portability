# Laravel Identification Rules

Confirmed Laravel application:
1. root composer.json exists;
2. composer require contains laravel/framework;
3. root artisan exists;
4. at least two of app/, bootstrap/, config/, routes/ exist.

Needs Review: laravel/framework is present but strong criteria are incomplete.

Initial eligibility: Confirmed Laravel + not archived + not fork + tests/ exists + composer.lock exists.
